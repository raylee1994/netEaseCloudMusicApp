const webpack = require("webpack");
const webpackD

var config = {
    mode: "development",
    optimization: {
        moduleIds: "named",
        chunkIds: "named",
        nodeEnv: "development"
    },
    devServer: {
        
    }
}